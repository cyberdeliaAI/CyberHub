# CyberHub base + bundled Gallery dependencies
Pillow>=9.0
send2trash>=1.8
requests>=2.28
watchdog>=4.0

# Optional modules carry modules/<id>/requirements.txt. The start scripts read
# those files only after the user has installed that module.
